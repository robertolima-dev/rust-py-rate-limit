//! Sliding Window algorithm — reserved for a future release (v0.3.0).
//!
//! Unlike Fixed Window, a sliding window smooths out bursts at the boundary
//! between windows by weighting the previous window's count. This module is a
//! placeholder so the layout matches the roadmap; it is not yet wired into the
//! extension (`lib.rs` does not declare `mod sliding_window`).
#![allow(dead_code)]
