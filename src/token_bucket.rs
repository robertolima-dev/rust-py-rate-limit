//! Token Bucket algorithm — reserved for a future release (v0.3.0).
//!
//! A token bucket refills at a steady rate and admits a request when a token is
//! available, allowing controlled bursts. This module is a placeholder so the
//! layout matches the roadmap; it is not yet wired into the extension
//! (`lib.rs` does not declare `mod token_bucket`).
#![allow(dead_code)]
